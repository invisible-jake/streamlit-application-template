# Python dependencies installed when the app builds on Databricks Apps.
# Pin versions (~= or ==) so builds are reproducible.

# --- Core UI ---
streamlit~=1.38.0
pandas~=2.2.3

# --- Databricks connectivity (uncomment when your app queries data) ---
# databricks-sdk~=0.30.0            # service-principal OAuth + WorkspaceClient
# databricks-sql-connector~=3.1.0   # run SQL against a SQL warehouse

# --- Other common extras (uncomment as needed) ---
# plotly~=5.24.0                    # richer interactive charts
# numpy~=2.1.0                      # numerical helpers
